[build]
builder = "NIXPACKS"
buildCommand = "echo 'Pre-built — nothing to build'"

[deploy]
startCommand = "node dist/index.mjs"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
